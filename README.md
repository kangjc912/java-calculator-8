# java-calculator-precourse
1. 빈 문자열을 받았을 때에는 0 return

2. 쉼표(,), 콜론(:)을 구분자로 가지는 문자열을 받았을 때, 구분자를 기준으로 분리한 각 숫자 합 return

3. 커스텀 구분자 지정(문자열 앞에 //와 \n 사이에 커스텀 구분자 지정 가능)

4. 잘못된 값(ex. 음수, 숫자가 아닌 문자열)을 입력받았을 때 예외처리